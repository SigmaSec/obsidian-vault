A type of attack that tries to overload the system and allows you to input once the computer can no longer handle the input stream of data

	Input size
	Return address
	overwrite a return address
	inject a shell into the buffer
