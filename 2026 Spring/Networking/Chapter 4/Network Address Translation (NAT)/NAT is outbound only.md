Allows many private hosts to share one public IP
NAT is stateful (tracks flows)
	NAT allows return traffic only if it matches an existing NAT table entry
A private resource can initiate connections to the internet, but the internet cannot initiate connections back to it.