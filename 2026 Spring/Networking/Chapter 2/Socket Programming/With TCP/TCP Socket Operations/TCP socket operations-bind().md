Host:
	A hostname, IP, or empty string
	empty string? server accepts all connections available
	if we use localhost or 127.0.0.1 only visible to same machine
Port
	Tcp port number accept clients
	it should be an integers from 1 to 65000