Minimize Disk I/O bottleneck
Transfer data from one disk to another 
Arrays stored in memory 
Binary Tree
Think of them as extra space to store a subset of the data but we are still using extra space
Need to use extra space to store each index
The more indexes you add the longer the runtime it takes. It has to update each index
If you have 50 indexes you run a insert, update, or delete, you have to do all of it again for each of the indexes
Normalization
DDL, DML, Creating table, insert, delete
